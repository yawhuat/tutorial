export default function Aboutlayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            <div className="h-24 w-full bg-blue-200">
                About the Layout Sample
            </div>
        </div>
    );
}